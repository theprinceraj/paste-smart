# Copy to .env and fill in.
#
# Your TypeSafe API key is not set here — it's entered once in the app's
# Settings window (tray icon → Settings…) and saved to a local config file by
# src-tauri/src/api.rs, not read from the environment.

# Optional overrides, read by Vite at build time (see envPrefix in vite.config.ts)
# and safe to expose to the webview bundle.
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
